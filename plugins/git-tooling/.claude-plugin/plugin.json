{
  "name": "git-tooling",
  "version": "1.8.0",
  "description": "Git tooling for Claude Code — worktree workflows, default-branch commit prompt, bulk worktree force-remove guard, force-push guard, PR-aware push reminders, and on-demand CI status watching with failing-check names surfaced inline",
  "author": {
    "name": "jedwards1230"
  }
}
