{
  "name": "git-tooling",
  "version": "1.13.0",
  "description": "Git tooling for Claude Code — default-branch commit prompt, bulk worktree force-remove guard, force-push guard, wt-done merged-branch cleanup, gh-resolve-threads PR review-thread triage, PR-aware push reminders, on-demand CI status watching with failing-check names surfaced inline, and release watching (GitHub Release + GHCR package publishes)",
  "author": {
    "name": "jedwards1230"
  }
}
